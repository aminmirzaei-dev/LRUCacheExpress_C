# LRUCacheExpress

LRUCacheExpress is a clean and efficient implementation of a fixed-capacity
Least Recently Used (LRU) cache written in pure C.

## Features And Options
- O(1) average-time get and put operations
- Modular design with reusable components
- Clear and minimal public API
